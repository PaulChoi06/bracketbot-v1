[project]
name = "bbos-daemon-slam"
version = "0.0.0"
requires-python = "==3.11.*"
dependencies = [
  "bbos",
  "numpy==2.4.6",
  "posix-ipc==1.3.2",
  "scipy",
]

[tool.uv]
package = false

[tool.uv.sources]
bbos = { path = "../../..", editable = true }
