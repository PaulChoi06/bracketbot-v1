[project]
name = "bbos-daemon-remote-session"
version = "0.0.0"
requires-python = "==3.11.*"
dependencies = [
  "bbos",
  "numpy==2.4.6",
  "posix-ipc==1.3.2",
  "opencv-python==4.13.0.92",
  "livekit==1.1.9",
  "httpx==0.28.1",
]

[tool.uv.sources]
bbos = { path = "../../..", editable = true }

[build-system]
requires = ["setuptools==80.9.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []
packages = []
